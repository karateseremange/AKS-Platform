# Installation — HQ-005.2 Sprint 3

Copier le contenu de `src` et `docs` dans le dépôt, puis publier avec `clasp push`.

Exécuter :

```javascript
AKS_runHQ0052Sprint3Tests()
```

Résultat attendu : 5 tests réussis sur 5.

Créer ensuite une nouvelle version du déploiement Web App et effectuer la recette depuis l'URL `/exec`.
