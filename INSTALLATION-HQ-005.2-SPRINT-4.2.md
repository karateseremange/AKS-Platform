# Installation — HQ-005.2 Sprint 4.2

1. Copier les fichiers du paquet dans le dépôt.
2. Exécuter `git diff --check` puis `clasp push`.
3. Exécuter `AKS_runHQ0052Sprint42Tests()` et les suites de non-régression.
4. Créer une nouvelle version du déploiement Web App.

Aucune migration Google Sheets n’est nécessaire.
