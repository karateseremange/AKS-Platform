# Installation HQ-005.2 — Sprint 1

Copier le contenu de l’archive à la racine du dépôt, vérifier les différences, puis exécuter `clasp push`.

Tests Apps Script :

- `AKS_runHQ0051Tests()` → 5/5 ;
- `AKS_runHQ0052Sprint1Tests()` → 3/3 ;
- toutes les suites précédentes doivent rester vertes.

La Web App existante doit ensuite être redéployée avec une nouvelle version pour tester l’URL `/exec`.
