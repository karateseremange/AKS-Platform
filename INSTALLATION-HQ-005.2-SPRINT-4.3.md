# Installation HQ-005.2 Sprint 4.3

1. Copier `src` et `docs` dans le dépôt.
2. Exécuter `clasp push`.
3. Exécuter `AKS_install()` : l'ancien `HQ_Submissions` est sauvegardé automatiquement si son schéma diffère.
4. Exécuter `AKS_runHQ0052Sprint43Tests()`.
5. Créer une nouvelle version du déploiement Web App.
