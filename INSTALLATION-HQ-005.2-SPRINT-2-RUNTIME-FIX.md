# Installation

Copier le dossier `src` à la racine du dépôt, puis exécuter `clasp push`.
Créer ensuite une nouvelle version du déploiement Web App et associer le déploiement `/exec` à cette version.

Test Apps Script : `AKS_runHQ0052RuntimeFixTests()`.
